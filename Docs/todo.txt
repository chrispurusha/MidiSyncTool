MidiSyncTool TODO

Things to do. ONE LINE PER ITEM - keep it that way.
Measurements, reasoning and completed-work narrative go in findings.txt, NOT here.
Built-but-unchecked work goes in to-test.txt.

Open decisions (see Docs/design.md)

- Does phase 1 need an audio input bus at all, or only from section 3 onward? Registering as an Fx from the start costs nothing and keeps it open
- Should MIDI clock run while the transport is stopped - always, never, or a setting?
- Monitor-only mode's reference: PARTLY ANSWERED 2026-09-03 - a MIDI clock input is now its own measurement (CLOCK INPUT section), separate from monitor mode's audio grid, and both can run at once. Still open is whether monitor mode should PREFER the MIDI clock as its reference when one is present
- Should the clock input drive the generated clock - a PLL that re-clocks an external master, the concept's section 2? Deliberately not built: measure first, and read the numbers before steering anything from them
- Does the timestamped MIDI send primitive go into SynthLib (all four projects benefit) or stay local until it settles?

Build

- First plug-in built 2026-09-02: loads, logs ProcessContext. NOT yet run in Live - see to-test.txt
- A UI "clear" for the host-timing figures: msStats.h has advertised one in a comment since it was written, only a transport start and the monitor boundary call ms_stats_reset() - it needs a UI-to-audio flag in tMsStatus, since a clear is an action and not a parameter
- Calibrate note-on/off latency as well as clock: send notes instead of clock and measure the audio onset the same way, so the device's note latency and its clock latency are separate data points rather than one blended figure
- Calibration needs to GUIDE the user (pattern, audio routing, MIDI port) and check each step, not present a CALIBRATE button that silently returns a wrong number
- mstDriver models loop wraps, tempo ramps (--ramp), varying block sizes (--vary) and Live's loop-boundary block split (--split, 2026-09-03): still needs playhead jumps while stopped
- Loop wrap: infer the wrap sample offset rather than trusting ppq, which snaps to the loop start for one block - see findings 2026-09-02
- A split block now carries the wrap's true sub-block offset in its own frame count; check whether that makes the inference above unnecessary at 512 frames and still needed at 256
- msStats still reports blockFramesMin/Max per CALL, so a splitting host reads "VARIES: 8..512"; decide whether that should report the device CYCLE with the split count beside it
- JUMP tolerance is 1e-6, too tight: a tempo ramp logs twenty lines of noise. Scale it to a fraction of a block
- Verify compensation end to end: needs the host's own audio as a reference, so QU-24 out 29 patched back to an input
- The harness round trip reads ~4.5 ms LONGER than Live's for the same rig; suspected capture-ring priming in mstDriver. Confirm, because compensation seeded from a harness run would over-compensate by about a block
- The first L + D of a run is uncompensated because the grid cannot be advanced before the transport starts; consider a pre-roll if it ever matters
- Subtract the constant audio-path terms so the figure can be called device latency rather than round trip - msDevice.c already reports them
- DECISIVE TEST for the Cirklon result: clock to the Elektron TM-1 (USB-to-DIN), DIN straight into the Tempest, no re-clocking sequencer. ~0.113 ms means the DIN hop did it and the Tempest's USB input was the jitter source; ~0.182 ms means the Cirklon's re-clocking did it
- MEASURE LIVE'S NATIVE CLOCK ON THE WIRE - the machinery now exists (Clock in row, findings 2026-09-03), the measurement does not: point Live's Sync at IAC, point Clock in at the same port, read the jitter; then ours the same way. The only defensible answer to "how much better is our clock"
- A/B direct to the Tempest over USB with the Cirklon OUT of the path: the only configuration where the filtering can show its worth without a re-clocker absorbing the difference first
- Repeat the A/B rows 3 and 4 of Docs/measurements.md - a 0.010 ms difference rests on one run each
- Re-measure the Tempest self-clocked case with the FIXED fit (its 0.132 ms was taken with the ordinal-index version; peak-to-RMS of 2:1 says no hits were dropped, but it should be confirmed)
- Re-measure the 0.113 ms (Cirklon) and 0.182 ms (USB) figures with the clean sample: both were taken against the drum-kit pattern now known to carry 8 ms outliers, so both are probably pessimistic
- Sweep velocity to quantify the onset detector's level bias, which is the one confound in the Cirklon comparison (input peak 0.0785 vs 0.0917, +1.35 dB)
- Audio loopback calibration: emit a click from the plug-in's own output and detect its return, measuring the interface path on its own with no drum machine in it
- Audio loopback is now the ONLY way to push the budget further: the wire is 1 % of total jitter and USB+Tempest+A/D is 98 % (findings 2026-09-03)
- Test whether USB framing is the 0.184 ms downstream term: uniform jitter over a 1 ms USB frame would be 0.289 ms RMS, and 0.184 is 0.64 of that
- Minimum-filter the model observation (NTP-style, lateness is one-sided) and/or a 2-state Kalman: both sound, both currently unobservable downstream - do not build until something downstream improves 10x
- The rate term costs ~0.006 ms of wire jitter and buys ~10 us of standing offset; revisit only if the wire ever becomes the dominant term again
- Sweep probe velocity and report the mean per velocity, which is the only way to know what the onset detector's level bias is worth on a given sound
- Onset detection is level-biased by design; measure how much by sweeping velocity once a pattern exists, and say whether it needs correcting
- Detector division is MST_DETECT_DIV until there is a panel; it should be derived from the pattern or chosen by the user
- CALIBRATE button on the panel: run the note probe and seed the compensation from what it measures - the applying half exists now ("Use measured", 2026-09-03), the RUNNING half does not
- ms_clock_set_compensation_ms is gated on compensationMs != 0.0, so with compensation off the schedule lead is never cancelled and the whole grid runs MS_LOOKAHEAD_MS late; and going from X back to 0 mid-run leaves the old advance in the phase until the next transport start
- Audio loopback to separate the interface's A/D from the device, so the breakdown's last row can be called device latency honestly
- msDevice.c and msRing.c are verbatim copies of GenBridge's - both sets belong in SynthLib once the audio/MIDI selector work is done
- Drift needs a long window to mean anything: tick quantisation alone moves it hundreds of ppm over a few seconds. Say so on the display rather than showing a figure that swings
